services:
  - type: web
    name: lhhcsa-hours
    env: python
    plan: free
    buildCommand: ""
    startCommand: gunicorn api.index:app --timeout 120 --workers 2
    envVars:
      - key: FLASK_ENV
        value: production
